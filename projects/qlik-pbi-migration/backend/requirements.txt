fastapi==0.115.0
uvicorn[standard]==0.31.0
anthropic==0.40.0
sqlalchemy==2.0.36
pydantic-settings==2.5.2
python-multipart==0.0.12
httpx==0.27.2
# docker-compose.yml points DATABASE_URL at Postgres, so this must stay uncommented
# for the containerized run. Local (non-Docker) runs fall back to sqlite via
# config.py's default and don't need it, but installing it doesn't hurt either way.
psycopg2-binary==2.9.10
# Uncomment for ArkhitX governance (Phase 3):
# arkhitx-sdk

# Only required when DATA_SOURCE=live (see docs/LIVE-EXTRACTION-SETUP.md) —
# used by services/qlik_extractor.py for the Engine API's direct WebSocket
# connection. httpx (already above) covers the QRS + Power BI REST calls.
websocket-client==1.8.0

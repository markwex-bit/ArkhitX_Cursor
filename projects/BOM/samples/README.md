# Sample data

Exported from `Tools/TPC Database - Synthesis Generator.xlsm` via `scripts/export_excel_tables.py`.

- **demo mode** (`DATA_MODE=demo`): uses `stacked_costbooks_demo.parquet` (3 vehicles)
- **full mode** (`DATA_MODE=full`): uses `stacked_costbooks.parquet` (~96k rows)

See `manifest.json` for row counts and column lists.

__pycache__/
*.py[cod]
.env
*.db
node_modules/
dist/
.vite/
data/

# Application projects — stay local or in separate repos, except an explicit
# allowlist below (used by the ArkhitX_Cursor remote, which mirrors the
# framework plus a chosen subset of projects).
/projects/*
!/projects/qlik-pbi-migration/
!/projects/BOM/

# Secrets
.env.local
*.env
!.env.example
